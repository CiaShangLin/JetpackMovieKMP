# 心得
沒想到ios端不支援Kotlin的Result，還要再用sealed轉換
一定要裝的套件skie ios端可以處理攜程跟flow，Android Studio的KMP插件，可以讓AS更好編譯IOS的部分
每次kmp這邊處理完還要再額外編譯ios有夠麻煩
kmp的module是每個module都支援kmp，跟之前想的不依樣，以為會像是android獨立的module
ios那邊要用module需要使用api去暴露出去，只引入一個共同引入的Shared App Module
ios的console好難用，不知道是xcode的問題還是我的問題，在底部的那一塊居然不會打印出來，結果是要自己開，預設居然沒開
ios這邊的debug沒辦法動態取得還要去env那邊設置argument
ios的xcode這邊再出現提示程式碼的時候真的有點兩光，一次都會幫你填上，然後有些提示程式碼還會打錯基本上在亂猜，跟AS的gemini差不多，我要還是需要copilot
paging kmp變成是在iosMain多寫一個presenter，是專門把paging從UI剝離出來，簡單來說就是把UI內部自己的操作開放出來給IOS用
xcode這邊頂部一次只能顯示一個檔案，沒辦法像AS那邊顯示多個，可能要研究一下設定有沒有可以改的，不然估計他是用左右切割
kotlin這邊的sealed需要做onEnum處理，int需要用int32處理再跳轉電影詳情頁的時候就出問題了

# KMP和iOS初次開發體驗
最近無聊去面試剛好他們有詢問到有沒有KMP相關經驗，因為公司專案實務上沒機會用到KMP所以一直沒有去研究，
剛好最近比較有時間就來體驗一下KMP的開發，再加上一起學一下iOS的SwiftUI開發，因為KMP把底層邏輯幾乎都做完了
不順便做的iOS有點可能，再加上Android的部分之前就寫過了，基本上是無痛遷移，Android的部分基本都交由AI去處理，
順便測試自己做的OpecSpec工作流擴展，之後可以用到公司的專案開發上面。

## KMP
第一次體驗KMP的開發，基本上是把底層邏輯都寫在Kotlin上面，然後再透過KMP的module去暴露給iOS使用，由於這一次在Android的部分
比較接近是遷移的部分，所以需要先研究轉換成KMP之後有哪些Library要替換掉，盡可能選用KMP通用支援的Library，這樣在iOS的部分
才不需要再額外處理，例如:Hilt->Koin，OKhttp+Retrofit->Ktor，像是DataStore，Room，Paging3這些也有提供KMP的版本所以就不需要額外替換。
KMP跟想像不依樣的地方，一開始建立模板專案的時候他裡面有一個Shared Module，裡面有分android的跟iOS的，一開始就有想說做模組化，
我以為是在這個Shared Module裡面時就建立其他Module，但是KMP的Module是每個Module都支援KMP，跟之前想的不依樣，以為會像是android獨立的module，這邊要注意一下。
其餘的我認為Android開發者在KMP開發上面應該是沒有什麼障礙的，用的還是Android Studio+Kotlin+Gradle。

### KMP 優點
對Android開發者友善
目前有滿多KMP的Library可以使用，像是Ktor，Koin，DataStore，Room，Paging3等等
效能比較好，不綁定UI，可以不使用CMP，也不像Flutter是全家桶
共用的底層邏輯可以讓其他平台使用，減少開發成本

### KMP 缺點
